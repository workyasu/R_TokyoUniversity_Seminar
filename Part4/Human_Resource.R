#????????????????????????
t=read.csv("HR_comma_sep.csv",header=T)

#???????????????
summary(t)

i=0
d=t
for(i in c(6,7)){
  d[,c(i)]=as.factor(t[,c(i)])
}
str(d)

sed.seed(123)
ind1=sample(nrow(d),floor(nrow(d)/50))
library(GGally)
ggpairs(d[ind1,],1:5,mapping=ggplot2::aes(color=left))